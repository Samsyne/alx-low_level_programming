#include <stdio.h>
/**
 * main - A program that prints its name, then followed by a new line
 *
 * @argc: counts number of command line arguments.
 * @argv: array vector that contains the command line arguments
 *
 * Return: 0 when it is success.
 */
int main(int argc  __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
	return (0);
}
